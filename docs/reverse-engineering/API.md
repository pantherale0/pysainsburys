# Sainsbury's GOL API Reference

> Static analysis of app v3.65.0 (`com.sainsburys.gol`). Live traffic validated 2026-08-30 — see [`PHASE1.md`](PHASE1.md).

## Architecture overview

The app uses **multiple HTTP backends** with Retrofit (OkHttp) and Ktor clients:

| Host | Role |
|---|---|
| `https://www.sainsburys.co.uk` | Primary grocery API (`/groceries-api/gol-services/...`) |
| `https://gol-app-bff-commerce.int.prd.jspaas.uk` | BFF proxy for basket/checkout/slot/order (feature-flagged) |
| `https://gol-app-bff-homepage.int.prd.jspaas.uk` | BFF proxy for homepage data |
| `https://account.sainsburys.co.uk` | OAuth 2.0 / OpenID Connect identity provider |
| `https://api.gs.sainsburys.co.uk` | SmartShop in-store API (`/shop/api/v1/...`) |
| `https://golapp-config.s3.amazonaws.com` | Remote app config (`/android/prod/config.json`) |
| `https://www.sainsburys.co.uk/smartlists/v1` | Smart lists |

Most grocery endpoints share the path prefix:

```
https://www.sainsburys.co.uk/groceries-api/gol-services/{service}/v{N}/...
```

When the BFF feature flag is enabled, an OkHttp interceptor rewrites select paths from `/groceries-api/gol-services/{slot,basket,checkout,order}/...` to the BFF commerce/homepage hosts, stripping the `/groceries-api/gol-services/` prefix.

---

## Authentication

### OAuth clients

| Channel | Client ID | Redirect URI | Extra authorize params |
|---|---|---|---|
| **Android app** | `gol-android` | `sainsburys://oauth/redirect-login` | `missionId=gol`, `audience=gol.sainsburys.co.uk`, `channel=Android`, `appVersion=<version>` |
| **Desktop web (gol-ui)** | `gol` | `https://www.sainsburys.co.uk/gol-ui/oauth/redirect` | `missionId=gol`, `audience=gol.sainsburys.co.uk`, `response_mode=query` |

Shared settings:

| Setting | Value |
|---|---|
| **Auth base URL** | `https://account.sainsburys.co.uk/` |
| **Discovery** | `https://account.sainsburys.co.uk/.well-known/openid-configuration` |
| **Authorization endpoint** | `https://account.sainsburys.co.uk/oauth2/auth` |
| **Token endpoint** | `https://account.sainsburys.co.uk/oauth2/token` |
| **Scopes** | `openid offline gol-session` |
| **PKCE** | `code_challenge` / `code_verifier`, method `S256` |

Identity stack uses **Ory Hydra** behind Akamai. Discovery/token endpoints may return 403 to unauthenticated `curl`; browsers and the app fetch discovery at runtime.

### Android login flow (AppAuth)

1. App opens browser/WebView to `account.sainsburys.co.uk/oauth2/auth` with the Android client params.
2. User authenticates; redirect returns authorization `code` to `sainsburys://oauth/redirect-login`.
3. App exchanges code for tokens at token endpoint (`access_token`, `refresh_token`, `id_token`, `expires_at`).
4. App calls **`POST /groceries-api/gol-services/login/v1/login-access-token`** to obtain commerce session tokens.

### Desktop web login flow (gol-ui)

Captured from Chrome DevTools (Aug 2026):

1. `GET /oauth2/auth?client_id=gol&redirect_uri=…/gol-ui/oauth/redirect&…` → Hydra login challenge.
2. `GET /gol/login?login_challenge=<uuid>` — login form (React login-ui).
3. `POST /gol/login` — form fields: `login_challenge`, `username`, `password`, `web_authn_device=0`, optional `ioBlackBox` (Iovation/Glassbox fraud signal).
4. If MFA enabled: `302` → `/gol/login/mfa`, then `POST /gol/login/send-mfa` to trigger code delivery, then `POST /gol/login/mfa` with `code` (+ optional `ioBlackBox`).
5. Success: `302` → `/oauth2/auth?…&login_verifier=<uuid>` → redirect to `https://www.sainsburys.co.uk/gol-ui/oauth/redirect?code=…&state=…`.
6. Exchange `code` at `/oauth2/token`, then commerce session via **`POST …/login/v1/login-access-token`**.

`pysainsburys` implements this via `GOLAuth` with `login()`, `request_mfa_code()`, `send_credentials()`, and `send_mfa_request()`. The `ioBlackBox` field is generated by third-party browser JS and may be required for automated login to succeed.

### Login access token exchange

**`POST /groceries-api/gol-services/login/v1/login-access-token`**

Request body (`LoginAccessPayload`):

```json
{
  "access_token": "<oauth_access_token>",
  "food_profile_create": true
}
```

Response (`WCSTokenEntity`):

```json
{
  "personalization_id": "string | null",
  "user_id": "string",
  "wc_token": "string | null",
  "wc_trusted_token": "string"
}
```

These WC tokens establish the WebSphere Commerce session used by subsequent grocery API calls.

### Authenticated request headers

The app attaches different header sets depending on context:

| Header | When | Source |
|---|---|---|
| `Authorization: Bearer <token>` | Authenticated grocery API calls | OAuth access token or refreshed token |
| `refresh_token: <token>` | Token refresh flows | Stored refresh token |
| `Cookie: <cookies>` | Session-bound requests | Cookie jar for `www.sainsburys.co.uk` |
| `WCAuthToken: <token>` | Commerce session | `{user_id},{wc_trusted_token}` URL-encoded (e.g. `682092082%2Cmcvf…`) |
| `User-Agent: GOLAppAndroid/<version>` | All grocery API calls | Hardcoded app version (live: `GOLAppAndroid/3.65.0`) |
| `X-acf-sensor-data: <sensor>` | Requests to pinned hosts | Akamai BMP SDK (`libakamaibmp.so`) |
| `X-SmartShop-Channel: gol` | `api.gs.sainsburys.co.uk` requests | Ktor plugin (hardcoded) |

**Pinned / sensor-protected hosts** (Akamai BMP applies `X-acf-sensor-data`):

- `www.sainsburys.co.uk`
- `account.sainsburys.co.uk`
- `api.gs.sainsburys.co.uk`
- `gol-app-bff-commerce.int.prd.jspaas.uk`
- `gol-app-bff-homepage.int.prd.jspaas.uk`
- (and CitrusAd, ChopChop, reviews hosts)

### Logout

**`DELETE /groceries-api/gol-services/login/logout`**

---

## Remote config

**`GET https://golapp-config.s3.amazonaws.com/android/prod/config.json`**

Response: `ConfigEntity` (feature flags, URLs, carousel config, etc.)

---

## Customer & identity

### Get customer profile

**`GET /groceries-api/gol-services/customer/v1/customer/profile`**

Response (`CustomerEntity`):

| Field | JSON key | Type |
|---|---|---|
| userId | `user_id` | string |
| customerId | `customer_id` | string |
| identityId | `identity_id` | string |
| email | — | string |
| familyName | `family_name` | string |
| givenName | `given_name` | string |
| primaryPhone | `primary_phone` | string |
| postcode | — | string |
| title | — | string |
| isVeryImportantCustomer | `is_very_important_customer` | boolean |
| deliveryPassExpiryDate | `delivery_pass_expiry_date` | date |
| dietaryProfileAttribute | `dietary_profile` | object |
| personalizationId | `personalization_id` | string |
| hasNectarAssociated | `has_nectar_associated` | boolean |
| hasNectarLinked | `has_nectar_linked` | boolean |
| isDigitalNectar | `is_digital_nectar` | boolean |

### Get identity profile

**`GET /groceries-api/gol-services/customer/v1/customer/identity-profile`**

Header: `Authorization: Bearer <token>`

Response (`IdentityProfileEntity`):

```json
{ "email": "user@example.com" }
```

### Addresses

| Method | Path | Body | Response |
|---|---|---|---|
| GET | `/groceries-api/gol-services/customer/v1/customer/address` | — | `AddressListEntity` |
| POST | `/groceries-api/gol-services/customer/v1/customer/address` | `AddressEntity` | empty |

---

## Basket

Base path: `/groceries-api/gol-services/basket/v2/basket`

| Method | Path | Query / Body | Response |
|---|---|---|---|
| GET | `/groceries-api/gol-services/basket/v2/basket` | `?calculate=true\|false&slot_booked=false` | `BasketEntity` |
| PUT | `/groceries-api/gol-services/basket/v2/basket` | `BasketUpdatePayload` | `BasketEntity` |
| DELETE | `/groceries-api/gol-services/basket/v2/basket` | — | empty |
| POST | `/groceries-api/gol-services/basket/v2/basket/item` | `AddToBasketPayload` | `BasketEntity` |
| DELETE | `/groceries-api/gol-services/basket/v2/basket/items` | `DeleteFromBasketPayload`, `?force_delete=` | `BasketEntity` |
| PUT | `/groceries-api/gol-services/basket/v2/basket/substitutions` | `ChangeSubstitutionsPreferencesPayload` | `BasketEntity` |
| POST | `/groceries-api/gol-services/basket/v2/basket/item/_addFromOrder` | `AddPreviousOrderItemsPayload` | `AddPreviousOrderToBasketEntity` |
| PUT | `/groceries-api/gol-services/basket/v2/basket/quick-add` | — | — |

### AddToBasketPayload

```json
{
  "product_uid": "string",
  "quantity": 1.0,
  "selected_catchweight": "string | null",
  "uom": "string"
}
```

### BasketEntity (response)

| Field | JSON key | Type |
|---|---|---|
| basketId | `basket_id` | string |
| orderId | `order_id` | string |
| subtotalPrice | `subtotal_price` | number |
| totalPrice | `total_price` | number |
| slotPrice | `slot_price` | number |
| savings | — | number |
| nectarSavings | `nectar_savings` | number |
| yourNectarSavings | `your_nectar_savings` | number |
| promoSavings | `promo_savings` | number |
| vouchersSavings | `vouchers_savings` | number |
| minimumSpend | `minimum_spend` | int |
| itemCount | `item_count` | int |
| deliveryInstructions | `delivery_instructions` | string |
| items | — | `BasketItemEntity[]` |
| isInAmendMode | `is_in_amend_mode` | boolean |
| slotType | `slot_type` | string |
| longestPromiseDate | `longest_promise_date` | string |
| isSlotLocked | `is_slot_locked` | boolean |
| error | — | `BasketErrorEntity[]` |
| hasExceededMinimumSpend | `has_exceeded_minimum_spend` | boolean |

---

## Products

Base path: `/groceries-api/gol-services/product/v1/product`

| Method | Path | Notes | Response |
|---|---|---|---|
| GET | `/product/{uid}` | `?include=` | `ProductEntity` |
| GET | `/product` | `?uid=`, `?filter[product_seo_url]=`, pagination | `SingleProductEntity` / `PaginatedProductListEntity` |
| GET | `/product/interstitial/{interstitial}` | | `SingleProductEntity` |
| GET | `/product/filter-by` | `page_number`, `page_size`, filter map | `PaginatedProductListEntity` |
| GET | `/product/complex-promotions` | `?promotionId=`, `?promotionText=` | `ComplexPromotionsEntity` |
| GET | `/product/your-nectar-prices` | | `SingleProductEntity` |
| GET | `/product/taxonomy` | | — |
| GET | `/product/meganav` | | — |
| POST | `/product/sponsored` | `SponsoredProductPayload` | `SponsoredProductsEntity` |
| POST | `/product/similar` | `SimilarProductPayload` | `SimilarProductsEntity` |
| GET | `/recommendations` | `?placement=`, `?limit=` | `RecommendationsEntity` |

Search suggestions (Ktor, not Retrofit):

```
GET https://www.sainsburys.co.uk/groceries-api/gol-services/product/v1/product/search/suggestions?search=<query>
```

---

## Favourites

| Method | Path | Body / Query | Response |
|---|---|---|---|
| GET | `/groceries-api/gol-services/product/v1/favourites` | pagination, filters | `PaginatedProductListEntity` |
| GET | `/groceries-api/gol-services/product/v1/favourites-by-pattern` | pattern params | `PaginatedProductListEntity` |
| POST | `/groceries-api/gol-services/product/v1/product/favourites` | `AddToFavouritesPayload` | empty |
| DELETE | `/groceries-api/gol-services/product/v1/favourites/{productSKU}` | `?sourceType=` | empty |

---

## Slots (delivery / collection)

| Method | Path | Body / Query | Response |
|---|---|---|---|
| GET | `/groceries-api/gol-services/slot/v1/slot/reservation` | `?order_uid=` | `SlotReservationEntity` |
| POST | `/groceries-api/gol-services/slot/v1/slot/reservation` | `SlotReservationPayload` / `ConfirmSlotPayload` | `SlotReservationResponseEntity` |
| GET | `/groceries-api/gol-services/slot/v1/slot/reservation/validate` | — | `SlotReservationResponseEntity` |
| GET | `/groceries-api/gol-services/slot/v1/slot/reservation/location-context` | — | `LocationContextEntity` |
| POST | `/groceries-api/gol-services/slot/v1/slot/reservation/address` | `SlotAddressPayload` | empty |
| POST | `/groceries-api/gol-services/slot/v2/slots` | `SlotPayload` (header: `X-Http-Method-Override: GET`) | `SlotWeekEntity` |

---

## Checkout

| Method | Path | Body / Query | Response |
|---|---|---|---|
| GET | `/groceries-api/gol-services/checkout/v2/checkout/start` | `?changes_trolley_confirmed=` | empty |
| GET | `/groceries-api/gol-services/checkout/v2/checkout/summary` | — | `CheckoutSummaryEntity` |
| POST | `/groceries-api/gol-services/checkout/v1/checkout/authorisation` | `AuthorisePaymentRequestEntity` | `AuthorisePaymentResponseEntity` |
| PUT | `/groceries-api/gol-services/checkout/v1/checkout/delivery-instructions` | `CheckoutDeliveryInstructionsPayload` | empty |
| POST | `/groceries-api/gol-services/checkout/v1/checkout/payment/start` | `AuthorisePaymentRequestEntity`, `referer` header | `StartPCIPaymentResponseEntity` |
| GET | `/groceries-api/gol-services/checkout/v1/checkout/payment/process` | `referer` header | `ProcessPCIPaymentResponseEntity` |
| GET | `/groceries-api/gol-services/checkout/v1/checkout/payment-cards/iframe` | — | `AddPaymentCardIFrameResponseEntity` |
| POST | `/groceries-api/gol-services/checkout/v1/checkout/retry-payment/start` | `RetryPaymentRequestEntity` | `StartRetryPaymentEntity` |

---

## Orders

| Method | Path | Query / Body | Response |
|---|---|---|---|
| GET | `/groceries-api/gol-services/order/v1/order` | `?page_number=`, `?page_size=` | `OrderParentEntity` |
| GET | `/groceries-api/gol-services/order/v1/order/{orderId}` | `?confirmation=`, `Citrus-Sessionid` header | `OrderEntity` |
| GET | `/groceries-api/gol-services/order/v1/order/status` | — | `OrderStatusEntity` |
| POST | `/groceries-api/gol-services/order/v1/order/{orderId}/amend` | — | empty |
| DELETE | `/groceries-api/gol-services/order/v1/order/amend` | — | empty |
| DELETE | `/groceries-api/gol-services/order/v1/order/{orderId}` | — | empty |

### OrderStatusEntity (response)

| Field | JSON key | Type |
|---|---|---|
| orderId | `order_uid` | string |
| isCutOff | `is_cutoff` | boolean |
| isInAmendMode | `is_in_amend_mode` | boolean |
| slotCutoffTime | `cutoff_time` | string |
| slotEndTime | `slot_end_time` | string |
| slotStartTime | `slot_start_time` | string |
| slotType | `order_type` | string |
| total | — | number |
| failedPayments | `failed_payments` | array |

---

## Nectar

| Method | Path | Response |
|---|---|---|
| GET | `/groceries-api/gol-services/nectar/v1/nectar/offers` | `NectarOffersEntity` |
| GET | `/groceries-api/gol-services/nectar/v1/nectar/ynp-opt-ins` | `YourNectarPriceOptInEntity` |
| PUT | `/groceries-api/gol-services/nectar/v1/nectar/ynp-opt-ins` | `UnlockYourNectarPriceEntity` |

Also: `GET https://api.gs.sainsburys.co.uk/nectar/v2/users/...`

---

## SmartShop (in-store) — barcode lookup

Base: `https://api.gs.sainsburys.co.uk`

SmartShop is the **only in-app path that resolves a scanned EAN to product data**. There is no
separate “lookup by barcode” endpoint: the app scans via ML Kit (`libbarhopper_v3.so`), then
**POSTs the raw EAN as `itemId`** to the basket API. The server resolves the product and returns
a `ShopDTO` whose `embedded.basket` contains line items with names, prices, etc.

### Required headers

| Header | Value | Notes |
|---|---|---|
| `Authorization` | `Bearer <OAuth access token>` | Same OIDC token as grocery API (`Session.getToken()`), **not** `WCAuthToken` |
| `X-SmartShop-Channel` | `gol` | Injected by Ktor plugin on all `api.gs.sainsburys.co.uk` requests |
| `X-Store-Id` | e.g. `0474` | Store context on basket/shop calls |
| `User-Agent` | `GOLAppAndroid/3.65.0` | Optional spoof: `sdk_gphone64_arm64, Android 35, SmartShop Android 3.66.1` (debug flag) |
| `Content-Type` | `application/json` | On POST/PATCH bodies |

### Shop lifecycle

| Method | Path | Body / query | Response |
|---|---|---|---|
| POST | `/shop/api/v1/shops` | `{"storeId":"0474"}` (`StoreIdDTO`) | Creates shop session → `ShopDTO` |
| GET | `/shop/api/v1/shops/{shopId}` | `?store_id=0474` | Shop detail |
| GET | `/shop/api/v1/shops/{shopId}/status` | — | Shop status |
| PATCH | `/shop/api/v1/shops/{shopId}` | `?store_id=0474&status=active` (etc.) | Update shop status |

### Basket / barcode

| Method | Path | Body | Notes |
|---|---|---|---|
| POST | `/shop/api/v1/shops/{shopId}/basket/items` | `{"storeId":"0474","itemId":"<EAN>","quantity":1}` (`AddItemDTO`) | **Primary barcode path** — `itemId` is the scanned EAN |
| PATCH | `/shop/api/v1/shops/{shopId}/basket/items/eans/{ean}` | `{"storeId":"0474","quantity":N}` (`UpdateQuantityDTO`) | Change quantity; `quantity: 0` likely removes |
| POST | `/shop/api/v1/shops/{shopId}/posmovements` | POS movement payload | Checkout / payment flow |

Response type: `ShopDTO { id, storeId, status, embedded: { basket: … } }`.

### Other SmartShop endpoints

| Method | Path | Notes |
|---|---|---|
| GET | `/identity/api/v1/users/me` | SmartShop user profile |
| GET | `/storeinfo/store?` | Store info by location |
| GET | `/nectar/v2/users/{id}/offers` | Nectar offers (in-store) |
| GET | `/nectar/v2/users/{id}/prices` | Nectar prices |
| GET | `/shop/api/v1/colleague/shop/{id}` | Colleague tooling |

### Local EAN cache

SmartShop maintains SQLite table `SmartShopEans` mapping **generic ↔ actual EAN** for loose
produce (barcodes that encode weight/price). Entries expire after 15 days.

### Live probe status (2026-08-30)

With a valid **web OAuth session** (`Bearer` + grocery cookies):

| Call | Result |
|---|---|
| `GET /shop/api/v1/shops` | **401** — Authorization missing or invalid |
| `POST /shop/api/v1/shops` | **403** — empty body |
| `GET /identity/api/v1/users/me` | **422** — token accepted but SmartShop profile not provisioned |

**No SmartShop traffic** appeared in the 2026-08-30 Frida browse capture (online tabs only).
A live in-store SmartShop session (QR confirmation at store entrance) is needed to capture the
working auth + shop-id + scan response shape.

---

## Product Finder (in-store search, not barcode)

Base: `https://www.sainsburys.co.uk/product-finder`

Separate UI feature (`ProductFinderFragment`) for **text search** with aisle/stock info in a
chosen store. Uses the same OAuth `Bearer` session as grocery API.

| Method | Path | Notes |
|---|---|---|
| GET | `/v1/products/store/{storeId}?keyword={q}&page={n}&size=20` | Legacy |
| GET | `/v2/products?storeId={storeId}&keyword={q}&page={n}&size=20` | Default when remote flag `ProductSearchV2/V3` active |
| GET | `/v3/products?storeId={storeId}&keyword={q}&page={n}&size=20` | Newer variant |
| GET | `/v2/products/batch?storeId={storeId}&productIds={uid,uid}` | Batch by **product UID**, not EAN |

API version selected by remote config flags (`ProductSearchV1` / `V2` / `V3`).

### Live probe status (2026-08-30)

| Query | Result |
|---|---|
| `keyword=milk`, `storeId=0474`, **`page=1`** | **200** — full product list with aisle/stock |
| `keyword=<EAN>`, same params | **200** — `content: []` (EAN not accepted as keyword) |
| **`page=0`** | **400** Validation failure |

Product Finder is useful for in-store text search but **does not expose barcode lookup**.

---

## Barcode lookup (online workaround)

Sainsbury's has **no public EAN search endpoint**. Products include `eans[]` in responses, but
lookup is only by UID or keyword.

### Recommended approach (pyukgroceries pattern)

Prior art: [pantherale0/pyukgroceries](https://github.com/pantherale0/pyukgroceries).

| Step | Service | Call |
|---|---|---|
| 1. Barcode → name | Open Food Facts | `GET https://world.openfoodfacts.org/api/v2/product/{ean}.json` |
| 2. Name → candidates | GOL product search | `GET …/product/v1/product?filter[keyword]={brand} {name}&page_number=1&page_size=24` |
| 3. Disambiguate | Client-side filter | Keep products where `{ean}` ∈ `eans[]` |

Search endpoint (legacy v1, still works unauthenticated):

```http
GET /groceries-api/gol-services/product/v1/product?page_number=1&page_size=24&filter[keyword]=QUERY
User-Agent: GOLAppAndroid/3.65.0
Accept: application/json
```

Response: `{ "products": [ { "product_uid", "name", "eans", "retail_price", … } ], "controls": … }`

**Validated:** EAN `8002270018213` → OFF name → keyword search → uid `6731637` with matching EAN.

**Caveats:** Open Food Facts coverage gaps; ambiguous names may return sibling SKUs until EAN
filter runs; loose-weight barcodes may not appear in OFF or Sainsbury's catalogue.

### Alternative: SmartShop (in-store only)

See SmartShop section above — native scan path, requires in-store session on `api.gs.sainsburys.co.uk`.

---

## Location

| Method | Path | Response |
|---|---|---|
| GET | `/groceries-api/gol-services/location/v1/location/click-and-collect` | — |
| GET | `https://api.gs.sainsburys.co.uk/storeinfo/store?` | Store info by location |

---

## Content / CMS

| Method | Path |
|---|---|
| GET | `/groceries-api/gol-services/content/v1/magnolia/template` |
| POST | `/groceries-api/gol-services/content/v2/withMagnoliaTemplate/ads` |

---

## BFF endpoints

When BFF routing is active, these paths are served from internal BFF hosts instead of `www.sainsburys.co.uk`:

- All basket, checkout, slot, and order paths listed above
- **`GET /bff/v1/home/data`** → `gol-app-bff-homepage.int.prd.jspaas.uk`

---

## Protections affecting direct API use

1. **Akamai BMP** — `X-acf-sensor-data` header required on protected hosts; generated by native SDK.
2. **Certificate pinning** — OkHttp `CertificatePinner` on grocery API clients.
3. **OAuth + WC tokens** — Two-layer auth: OIDC bearer token + commerce session (`WCAuthToken`, cookies).
4. **Play Integrity** — Referenced for device attestation (exact enforcement TBD via live capture).

---

## Validation status

| Item | Static | Live capture |
|---|---|---|
| Endpoint paths | ✅ From Retrofit interfaces | ✅ 183 requests captured (Frida, 2026-08-30) |
| Request/response schemas | ✅ From Gson model classes | ✅ Live samples in [`samples/`](samples/) |
| Auth header composition | ✅ From interceptors | ✅ Validated (see below) |
| Akamai sensor format | ⚠️ Header name only | ⚠️ `ak_bmsc` cookie validated; `X-acf-sensor-data` not on browse traffic |
| OAuth discovery URLs | ✅ Static + inferred (Ory Hydra) | ⚠️ Akamai blocks external fetch; app uses runtime discovery |
| Session cookies post-login | ✅ Frida CookieManager dump | ✅ Validated |
| Customer / commerce user IDs | ✅ `gol_plain_preferences` + cookies | ✅ Validated |
| Live request headers | ✅ Frida OkHttp hook | ✅ Validated |

### Live-validated endpoints (2026-08-30)

Captured via `artifacts/scripts/run_live_capture.py` after login (Frida + OkHttp hook):

| Method | Path | Notes |
|---|---|---|
| GET | `/groceries-api/gol-services/product/v1/favourites` | Query: `page_number`, `page_size`, `store_identifier`, `slot_type`, `personalization_id` |
| GET | `/groceries-api/gol-services/product/v1/favourites-by-pattern` | Query: `pattern=seasonal\|usuals` |
| GET | `/groceries-api/gol-services/product/v1/recommendations` | Query: `placement=newness&limit=20` |
| GET | `/groceries-api/gol-services/product/v1/product/meganav` | Shop tab navigation |
| GET | `/groceries-api/gol-services/product/v1/product/taxonomy` | Category tree |
| GET | `/groceries-api/gol-services/basket/v2/basket` | Query: `calculate=true&store_number=0474&slot_booked=false` |
| GET | `/groceries-api/gol-services/customer/v1/customer/address` | Saved addresses |
| GET | `/groceries-api/gol-services/order/v1/order` | Order history |
| GET | `/groceries-api/gol-services/content/v2/withMagnoliaTemplate/ads` | Homepage ads |

**Confirmed authenticated headers** on grocery API GETs:

```
Authorization: Bearer <JWT RS256, ~1229 chars>
WCAuthToken: 682092082%2C<wc_trusted_token>
Cookie: ak_bmsc=…; WC_AUTHENTICATION_682092082=…; JSESSIONID=…; …
User-Agent: GOLAppAndroid/3.65.0
```

`refresh_token` header was not sent on routine GETs (only expected on token-refresh calls).

### Live response samples

See [`samples/`](samples/) and [`PHASE1.md`](PHASE1.md) for captured JSON excerpts (favourites, orders, remote config).
